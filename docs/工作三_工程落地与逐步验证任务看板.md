# 工作三：工程落地与逐步验证任务看板（Task Kanban）

> **文档用途**：作为工作三（基于状态表征学习的多架次飞机装配动态协同调度）从理论设计向工程代码落地的**总任务追踪看板**。供后续专业编程智能体逐项认领、实施、测试与状态流转。
>
> **基准依据**：本看板严格依据 2026 年 9 月 19—20 日最新确定的 6 份阶段基准文件（尤其是《工作三_方法设计阶段确认稿》与《工作三_实验设计阶段确认稿》）编制。
>
> **任务状态流转规范**：
> - `⚪ [未规划]` (Backlog)：暂未细化具体实现的远期任务；
> - `🟡 [已计划待实施]` (Planned)：技术方案与通过标准已锁定，可直接由编程智能体认领实施；
> - `🔵 [已实施待验证]` (Implemented)：代码已完成编写，单元测试脚本已就绪，等待运行核验；
> - `🟢 [已验证闭环]` (Verified)：单元测试 100% 通过，断言日志与账本核验无误，产出符合预期。

---

## 阶段验收里程碑总览（Milestones）

| 里程碑 | 核心验证重点 | 对应任务范围 | 验收通过标准 | 当前状态 |
| :--- | :--- | :--- | :--- | :---: |
| **M1: 仿真流程跑通** | 小规模五站实例：填线、预约、后移、同步转站、排空 | Task 1.1 ~ Task 2.7 | 无资源冲突；飞机到站后才能加工；实际全部完工才自然终止；正好触发 14 次转站 | 🟢 验收通过 |
| **M2: 奖励计账闭环** | 节拍超期、周期内偏差、换人比例、改派惩罚 | Task 3.1 ~ Task 3.3 | 单步奖励累加值 $\sum r_n^{\text{actual}}$ 与终局目标 $-J_{\text{total}}$ 绝对一致（误差 $< 10^{-6}$） | 🟢 验收通过 |
| **M3: 扰动与易错点** | 9类解耦场景、五站覆盖、三大易错点攻坚 | Task 4.1 ~ Task 4.4 | 旧预约开工事件正确失效；后移仍保留恢复时间；采样截断不误作生产结束 | 🟢 验收通过 |
| **M4: 时间修正头验证** | 收集验证轨迹，训练预测残差 | Task 5.1 ~ Task 6.4 | 修正后时间估计 $\widehat P_q$ 的 MAE 相比原始启发式下降 15% 以上；势函数保策略不变性 | 🟡 待实施 |
| **M5: PPO训练与对比** | 条件分支自回归 PPO 训练，方法 D vs 基线 C | Task 7.1 ~ Task 7.4 | 条件分支动作概率重放正确；方法 D 协同性能优于基线 C，输出首份对比报表 | 🟡 待实施 |

---

## 模块一：独立目录搭建与多架次基准数据展开

*目标：建立完全隔离的代码目录，并将成文论文的 `real_283` 基准模板自动展开为 10 架次多机流水计划。*

| 任务编号 | 状态 | 任务名称 | 具体实现细节与代码落点 | 交付产物与验证标准 |
| :---: | :---: | :--- | :--- | :--- |
| **Task 1.1** | 🟢 已验证闭环 | 创建工作三专用目录树 | 新建 `envs/work3/`、`utils/work3/`、`tests/work3/`、`models/work3/`、`data/work3/`、`scripts/work3/` | 目录结构创建完毕，Python 包导入路径配置无冲突 |
| **Task 1.2** | 🟢 已验证闭环 | 多架次基线展开器编写 | 编写 `utils/work3/multi_aircraft_baseline.py`：<br>读取 `data/real/real_283_baseline.csv`，将单模板 $H_0$ 按 $K=10$ 架次展开：<br>- 飞机编号 $k \in [0, 9]$，名义投产时刻 $k \cdot H_0$<br>- 任务唯一身份定义为二元组 $(k, i)$，总工序数 $10 \times 283 = 2,830$<br>- 基础站位 $m_{ki}^0 = m_i^0$，基准开工 $S_{ki}^0 = (k + m_i^0 - 1)H_0 + b_i^0$ | 生成 `data/work3/real_283_k10_baseline.json`，格式严密规范 |
| **Task 1.3** | 🟢 已验证闭环 | 多架次基线静态校验 | 编写 `tests/work3/test_baseline_k10.py`：<br>对展开后的 2,830 道工序执行硬约束静态扫描（工艺 DAG 偏序、站位空间上限、固定工人归属、技能要求） | 运行 `pytest tests/work3/test_baseline_k10.py` 通过，确认基准计划物理违规数为 0 |

---

## 模块二：单一事件驱动多架次仿真引擎（完成里程碑 M1）

*目标：构建多机位追踪、条件动作、可修改预约与同步脉动状态机，获取第一条无冲突的完整生产轨迹。*

| 任务编号 | 状态 | 任务名称 | 具体实现细节与代码落点 | 交付产物与验证标准 |
| :---: | :---: | :--- | :--- | :--- |
| **Task 2.1** | 🟢 已验证闭环 | 核心数据结构与状态维护 | 在 `envs/work3/core_types.py` 中定义多机状态类：<br>- 维护 10 架飞机的物理站位数组 $s_k(t) \in \{-1, 0, 1, 2, 3, 4, 5\}$<br>- 任务状态严格区分：`UNREADY` (0), `READY` (1), `RESERVED` (2), `RUNNING` (3), `COMPLETED` (4), `POSTPONED` (5)<br>- 维护固定工人占用日历与 5 个站位的物理在场飞机 | 数据结构就绪，支持状态快照、复原与重置 |
| **Task 2.2** | 🟢 已验证闭环 | 最小离散事件队列引擎 | 编写 `envs/work3/event_queue.py` 实现优先队列（`heapq`），统一支持核心生产事件，内置代数标记（Generation Token）失效机制 | 事件推进器无时间倒退，按时序精确推进，旧预约失效 100% 拦截 |
| **Task 2.3** | 🟢 已验证闭环 | 留在当前站动作逻辑 | 实现动作分支 A（留在当前站 $s_k$）：<br>- 选定本站就绪工序 $(k,i)$<br>- 在固定于站位 $s_k$ 的工人中选派满足资质的团队 $W$<br>- 二元对齐：$\alpha=0$ 最早可行；$\alpha=1$ 增加 $P_{q-1} + b_i^0$ 下界<br>- 若资源暂忙或已知到料在未来，登记为 `RESERVED`，不阻塞其他决策 | 工人暂忙时成功建立未来预约，开工时转为 `RUNNING` |
| **Task 2.4** | 🟢 已验证闭环 | 合法后移分支与放行校验 | 实现动作分支 B（后移至 $s_k+1$）：<br>- 检查工艺站位资格、放行必完要求、后继依赖<br>- **动作截断**：当场不选队、不占工人、不选对齐<br>- 标记为 `POSTPONED`，移出当前站必须完工集合，计入改派次数 $n_{ki}$ | 非法后移被动作掩码刚性拦截；合法后移当场截断成功 |
| **Task 2.5** | 🟢 已验证闭环 | 全线同步脉动判定与推进 | 实现转站判定：<br>- 检查站位 $0 \sim 4$：归属本站的所有未后移任务均已 `COMPLETED` 且放行条件满足<br>- 触发 `SYNCHRONOUS_TRANSFER`：各机位置更新 $s_k \leftarrow s_k+1$，记录实际转站时刻 $P_q = \max(P_{q-1} + H_0, \max_s F_{s,q})$，节拍跨度 $H_q = P_q - P_{q-1}$<br>- 飞机转站到达新站位后，其先前后移的任务自动解除 `POSTPONED`，进入新站任务池 | 成功观测到 5 站同时步进，后移工序在新站被重新唤醒 |
| **Task 2.6** | 🟢 已验证闭环 | 生产自然终止与采样截断 | 明确终止条件：<br>- `terminated = True`：第 9 号飞机完全离开第 4 站，2,830 道工序 100% 完成<br>- `truncated = True`：仅用于 PPO rollout 步数达到上限，此时产线状态与事件队列保持，供 Critic bootstrap | 绝不在中间转站时错误退出环境 |
| **Task 2.7** | 🟢 已验证闭环 | 首条完整无扰动轨迹跑通 | 编写 `tests/work3/test_pipeline_run.py`：<br>使用贪心规则跑通全流程（5 周期填线 $\to$ 5 周期满载 $\to$ 4 周期排空） | **【里程碑 M1 验收通过】**：输出完整 14 周期汇总数据，确认无工人冲突，正好触发 14 次脉动并完工 |

---

## 模块三：奖励计账与目标函数闭环（完成里程碑 M2）

*目标：构建独立终局账本与单步增量奖励，数学证明两者完全一致，无重复或遗漏计费。*

| 任务编号 | 状态 | 任务名称 | 具体实现细节与代码落点 | 交付产物与验证标准 |
| :---: | :---: | :--- | :--- | :--- |
| **Task 3.1** | 🟢 已验证闭环 | 真实综合目标独立计算器 | 编写 `utils/work3/objective_evaluator.py`，从轨迹日志直接结算：<br>- 节拍超期：$J_{\text{takt}} = \sum_{q=1}^{14} [H_q - H_0]_+ / H_0$<br>- 周期内位置偏差：$D_{\text{time}} = \sum_{k,i} \|(S_{ki} - P_{q-1}) - b_i^0\| / H_0$<br>- 团队替换率：$D_{\text{team}} = \frac{1}{N}\sum (1 - \|W \cap W^0\|/m)$<br>- 改派惩罚：$\sum g(n_{ki}) = \lambda_1 n + \lambda_2 [n-1]_+$ | 独立账本函数完成，分项指标输出清晰，支持外部日志复核 |
| **Task 3.2** | 🟢 已验证闭环 | 单步奖励计算与增量计价 | 在环境 `step()` 中实现 $r_n^{\text{actual}} = -(A_{n+1} - A_n)$：<br>- 正式改派时扣减本次改站增量代价<br>- 实际开工确认一次基准开工偏差与团队偏差<br>- 实际脉动转站时结算一次本周期节拍超期 | 单步奖励即时返还，不预先多重扣罚，不在后续步重复扣费 |
| **Task 3.3** | 🟢 已验证闭环 | 账本数学等价性单元测试 | 编写 `tests/work3/test_reward_consistency.py`：<br>随机动作运行多条完整轨迹，比对 $\sum r_n^{\text{actual}}$ 与 $-J_{\text{total}}$ | **【里程碑 M2 验收通过】**：断言误差小于 $10^{-6}$，证明计账逻辑 100% 严密自洽 |

---

## 模块四：9类解耦扰动生成与三大易错点排查（完成里程碑 M3）

*目标：实现 3时机×3强度 正交场景库，硬核排查并修复旧事件失效、后移欺骗与中间势值清零三大漏洞。*

| 任务编号 | 状态 | 任务名称 | 具体实现细节与代码落点 | 交付产物与验证标准 |
| :---: | :---: | :--- | :--- | :--- |
| **Task 4.1** | 🟢 已验证闭环 | 9类解耦场景库生成器 | 编写 `utils/work3/disturbance_generator.py`：<br>- 锚定稳态第 6 周期内部：早 ($0.225 H_0$)、中 ($0.400 H_0$)、晚 ($0.575 H_0$)<br>- 解耦延迟强度：$\Delta_L = 0.15 H_0, \Delta_M = 0.35 H_0, \Delta_H = 0.60 H_0$<br>- 统一直接建模工序开工可用性延迟：$r_{k^*,i}^{\text{new}} = \max(r_{k^*,i}^{\text{old}}, R)$<br>- 受扰飞机涵盖 5 个站位，全算法复用离线确定性场景 | 生成锁定的 9 类场景数据集 `data/work3/scenarios_9class.json` |
| **Task 4.2** | 🟢 已验证闭环 | **易错点 1 攻坚：旧预约事件失效** | 在环境物料到达处理中实现：<br>当扰动在 $\tau$ 揭示且将恢复时间推迟至 $R$ 时，**遍历已建立预约的任务**：<br>若任务 $(k,i)$ 原定预约开工时刻 $t_{\text{sched}} < R$，**必须立刻取消该工序在事件队列中的未来开工事件，释放工人预占日历，并将工序退回 `UNREADY` 状态** | 编写 `tests/work3/test_appointed_invalidation.py`，断言绝对不会发生“工序在到料时刻 $R$ 之前按原预约违规开工” |
| **Task 4.3** | 🟢 已验证闭环 | **易错点 2 攻坚：后移保留物料约束** | 完善后移状态继承：<br>当受扰任务 $(k^*,i)$ 被后移至 $s_k+1$ 时，其 $r_{k^*,i}^{\text{new}} = R$ 属性永久保留。当飞机到达下一站重新派工时，开工下界依然受限为 $\ge R$ | 编写 `tests/work3/test_postpone_retention.py`，断言后移工序绝对无法逃避 $R$ 约束提早开工 |
| **Task 4.4** | 🟢 已验证闭环 | **易错点 3 攻坚：规范采样截断接口** | 封装环境与 Rollout 交互接口：<br>- 脉动转站时不 reset 环境，不切断势函数；<br>- Rollout 截断时返回 `truncated=True`，下一次采样从当前现场状态无缝续接 | **【里程碑 M3 验收通过】**：在极端大扰动下跑通 9 类场景，三大易错点全部通过断言 |

---

## 模块五：启发式基座与多架次轨迹收集（为里程碑 M4 准备数据）

*目标：构建多架次异构图表征与当前周期启发式完工估计，跑通基准 C 策略并收集验证轨迹。*

| 任务编号 | 状态 | 任务名称 | 具体实现细节与代码落点 | 交付产物与验证标准 |
| :---: | :---: | :--- | :--- | :--- |
| **Task 5.1** | 🟢 已验证闭环 | 多架次异构图构造器 | 编写 `models/work3/graph_builder.py`：<br>- 节点包含当前在制任务节点 $(k,i)$、五站物理站位节点、固定工人节点与 5 个 Skill Hub 节点<br>- 边包含工序依赖、站位归属、有效执行/预约占用以及双向 Skill Hub 资源边<br>- 显式注入飞机架次相对位置、物理相对站位偏移 $(s_k - m_i^0)$ 等特征 | 异构图数据 `HeteroData` 4类节点5类边完整连接，支持动态动作掩码 |
| **Task 5.2** | 🟡 已计划待实施 | 周期启发式完工时间估计器 | 移植并改造当前周期的静态估计器 $\widehat P_q^h(s)$：<br>基于当前部分排程、在制工序完工时间、站位当前剩余标准工时及工人供给，输出本周期无学习的基线完成时刻 | 启发式估计函数就绪，能快速给出基准参考时间 |
| **Task 5.3** | 🟡 已计划待实施 | 基线 C 策略运行器 | 编写 `models/work3/heuristic_agent.py`：<br>实现允许合法后移的启发式调度基线 C（优先站内处理，严重滞后且合法时后移），用于初期生产验证 | 基线 C 能自主在 9 类扰动下完成全线装配 |
| **Task 5.4** | 🟡 已计划待实施 | 训练轨迹数据集收集 | 编写 `scripts/work3/collect_validation_trajectories.py`：<br>运行基线 C 收集 50 条完整生产轨迹，落盘保存元组 $(s_n, \widehat P_{q_n}^h, P_{q_n}^{\text{actual}})$ | 产出供时间修正头离线训练与验证的数据集 `data/work3/val_trajectories.pt` |

---

## 模块六：时间残差预测头与势函数塑形（完成里程碑 M4）

*目标：训练时间修正头，在独立验证轨迹上数学证明残差预测显著优于原始启发式，构建势函数。*

| 任务编号 | 状态 | 任务名称 | 具体实现细节与代码落点 | 交付产物与验证标准 |
| :---: | :---: | :--- | :--- | :--- |
| **Task 6.1** | 🟡 已计划待实施 | 时间修正头架构设计 | 在 Actor 图编码器 $z = f_\theta(s)$ 上挂载轻量级时间修正头 $\delta_\psi(s)$（2层MLP）：<br>预测归一化时间残差，得到修正时间 $\widehat P_q = \max\{t, \widehat P_q^h + H_0 \delta_\psi(s)\}$ | 模块定义完成，计算图支持反向传播 |
| **Task 6.2** | 🟡 已计划待实施 | 辅助回归训练与离线验证 | 编写 `models/work3/train_time_head.py`：<br>使用真实转站标签 $y = (P_q - \widehat P_q^h)/H_0$ 进行 Huber 损失训练；在留出验证集上对比 $\widehat P_q$ 与 $\widehat P_q^h$ 的 MAE 误差 | **【里程碑 M4 验收通过】**：验证集上修正后的 MAE 相比原始启发式下降 15% 以上 |
| **Task 6.3** | 🟡 已计划待实施 | 动作头显式时间特征融合 | 将标量特征 $[\widehat R/H_0, (H_0 - (t - P_{q-1}))/H_0]$ 经融合层拼入动作决策上下文，使策略感知时间紧迫度 | 动作头接口就绪，向前推理输出合法分布 |
| **Task 6.4** | 🟡 已计划待实施 | 动态势函数奖励塑形器 | 编写 `models/work3/potential_shaping.py`：<br>实现 $\Phi(s) = -a \widehat H_q/H_0 - b [\widehat H_q - H_0]_+/H_0$<br>返回步步塑形奖励 $r_n^{\text{step}} = r_n^{\text{actual}} + \beta \{\gamma \Phi(s_{n+1}) - \Phi(s_n)\}$ | 势函数差分器完成，且在轨迹内固定预测器副本 |

---

## 模块七：条件分支 PPO 训练与小规模对比（完成里程碑 M5）

*目标：实现条件分支的精确 PPO 概率重放，小规模对比完整方法 D vs 基线 C，输出第一张科研对标表。*

| 任务编号 | 状态 | 任务名称 | 具体实现细节与代码落点 | 交付产物与验证标准 |
| :---: | :---: | :--- | :--- | :--- |
| **Task 7.1** | 🟡 已计划待实施 | 条件分支自回归解码网络 | 编写 `models/work3/actor_critic.py`：<br>- 工序选择头 $\to$ 站位二选一头<br>- 若留在当前站：激活选队指针头序列与二元对齐头；<br>- 若后移：截断动作，**PPO 对数概率仅累加工序与站位两项** | 单元测试验证动作掩码与重放概率数学严格一致 |
| **Task 7.2** | 🟡 已计划待实施 | PPO 训练主循环与版本管理 | 编写 `train_work3.py`：<br>正确实现经验回放池、辅助损失反向更新 Actor 编码器、塑形预测器版本冻结机制 | 训练循环跑通，无显存泄漏或张量维度错位 |
| **Task 7.3** | 🟡 已计划待实施 | 基线 C 与方法 D 小规模对比训练 | 先训练无时间辅助的基线 C，再训练完整方法 D（残差预测＋动作辅助输入＋势函数），各运行 3 个种子 | 获得收敛曲线与模型检查点 |
| **Task 7.4** | 🟡 已计划待实施 | 首轮实验结果统计与报表生成 | 编写 `scripts/work3/evaluate_c_vs_d.py`：<br>在 9 类测试场景上评估完工节拍超期、开工位置偏差与改派稳定性 | **【里程碑 M5 验收通过】**：输出首份完整的 C vs D 实验对比表与总结分析 |
